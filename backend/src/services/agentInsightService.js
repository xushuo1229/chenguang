'use strict';

const {
  ACTION_LEVEL,
  INSIGHT_VERSION,
  MAX_INSIGHTS,
  buildInsights,
} = require('./agentInsights/insightEngine');

module.exports = {
  ACTION_LEVEL,
  INSIGHT_VERSION,
  MAX_INSIGHTS,
  buildInsights,
};
