# 晨光自律台 · Autonomous User Validation Simulation

> 阶段：BETA-TEST  
> 基线：`587f67c`  
> 方法：按 10 条大学生典型旅程做代码级、页面级与数据流级模拟；本报告是产品判断依据，不虚构真实用户统计。  
> 修复边界：只允许调整 UX、文案、交互、空状态、引导与 AI 体验；禁止架构重写、大型功能与数据库模型变更。

## 1. 模拟画像与旅程结果

| # | 用户画像 | 旅程要点 | 主要发现 | 分级 |
|---|---|---|---|---|
| 1 | 大一 · 基础课适应型 | Landing → 注册 → 课程 → 待办 → 打卡 → 统计 | Landing 价值清楚；工作台可一次看到课程、待办与打卡；空统计缺少直接返回记录的按钮 | P1 |
| 2 | 大一 · 社团生活型 | 注册 → 目标 → 阅读 → 待办 → AI | 新手引导包含目标；但引导关闭只用右上角 ✕，忙碌新生可能误以为必须完成 | P2 |
| 3 | 考研 · 冲刺型 | 登录 → 深度专注 → 待办 → 统计 → AI | 专注、待办、趋势链路完整；AI 有真实上下文与只读边界 | 通过 |
| 4 | 考研 · 基础期型 | 目标 → 阅读 → 专注 → AI | 目标进度能由真实记录计算；首目标创建后缺少“现在去记录”的显式承接 | P1 |
| 5 | 健身 · 增肌型 | 注册 → 运动目标 → 运动记录 → 统计 | 运动卡全宽，记录路径直接；产品缺少“个人最好成绩”类反馈，但属于后续功能，不本轮开发 | P3 |
| 6 | 健身 · 恢复期型 | 低强度记录 → 统计 → AI | 空态已避免“必须高强度”暗示；AI 可给温和建议 | 通过 |
| 7 | 自律失败 · 重启型 | 注册 → 小目标 → 10 分钟任务 → AI | 下一步行动能指向 10 分钟小事；但连续记录为 0 时，文案可能加重压力 | P3 |
| 8 | 自律失败 · 高目标型 | 设定高目标 → 记录不足 → AI | AI 能承认数据不足，不编造结果；风险提醒依赖真实记录 | 通过 |
| 9 | 高效规划 · 考试周型 | 批量待办 → 专注 → 统计 → AI | 待办完成率与趋势清晰；下一步行动优先展示第一个未完成项 | 通过 |
| 10 | 忙碌 · 实习课业型 | 快速注册 → 一件小事 → 移动端 → AI | 可快速跳过复杂模块；但引导的“跳过”不够明显，首屏信息仍偏多 | P2 |

## 2. 分级结论

### P0

未发现阻塞注册、登录、目标创建、行为记录、数据同步或 AI 调用的缺陷。

### P1

1. **空统计页缺少行动入口**  
   页面已经说明“完成一次学习、运动或专注打卡后……”，但没有直接按钮。用户需要自行寻找工作台。  
   修复：为空统计页增加“去完成第一个记录”主按钮，跳转工作台。

2. **首目标创建后缺少轻量承接**  
   目标创建成功后，用户知道目标已经建立，但最容易流失的下一步是“回工作台做第一次记录”。  
   修复：把成功文案改为“目标已创建，回工作台完成第一次记录”，不新增弹窗或功能。

### P2

1. **新用户引导的跳过方式不够自然**  
   当前只有右上角 ✕，忙碌学生或只想先体验一个模块的学生可能不确定能跳过。  
   修复：把关闭按钮语义改为明确的“跳过引导”，保留原有关闭行为。

2. **忙碌用户首屏信息量偏高**  
   工作台模块丰富，但对只想记录一件事的用户可能造成轻微认知负担。  
   修复方向：后续可继续用文案与默认展开层级控制密度，不删除模块。

3. **AI 快捷问题偏通用**  
   对考研、健身、忙碌学生都可用，但个性化程度有提升空间。  
   修复方向：保持当前 AI 定位，后续仅在真实 Beta 反馈明确后再调整。

### P3

1. **连续记录为 0 可能带来轻微压力**  
   对重启用户可再弱化“从零开始”的语气。
2. **健身用户缺少个人最好成绩反馈**  
   有长期价值，但属于新功能，不符合本轮边界。
3. **目标表单对新手仍偏结构化**  
   字段均有默认值，不是阻塞，但真实 Beta 中需观察是否能被理解。

## 3. 本轮修复计划

| 优先级 | 修复 | 允许范围 |
|---|---|---|
| P1 | 空统计页增加直接行动按钮 | UX / 空状态 |
| P1 | 首目标创建文案补充下一步 | 文案 |
| P2 | 新手引导关闭按钮改为明确跳过 | 交互 / 引导 |
| 验证 | 前端 / 后端 / 构建 / 安全 / UX 回归 | 全部只读验证 |

## 4. 不修改的内容

- 不新增统计图表、成就系统、社交功能或模板计划。
- 不修改 `store.js`、`sync.js`、数据库模型、认证模型或同步协议。
- 不把 AI 改成开放聊天机器人，不虚构用户数据或模型能力。
- 不为不同画像创建独立产品模式，避免增加第二套逻辑。

## 5. Beta 就绪初判

产品可以进入真实 Beta 测试，但应控制为 10 人左右的小规模受控试用。当前最大的不确定性不是功能缺失，而是三类真实反馈：

1. 忙碌用户是否愿意每天至少完成一次最小记录。
2. AI 建议是否在真实数据量较小时仍然有用。
3. 目标表单与统计页是否被大一或非高效型用户理解。

