# 晨光自律台 · Final Real Beta Ready Report

> 阶段：REAL-BETA-OPS  
> 基线：`dfd17d0`  
> 结论：**可以邀请 10–15 名真实大学生进入受控 Beta。**  
> 本阶段只补齐测试运营能力，不新增用户功能、不改核心架构、不改数据库模型。

## 1. 本阶段交付

| 交付物 | 用途 |
|---|---|
| `BETA_TEST_PLAN.md` | 14 天、10–15 人受控测试方案 |
| `BETA_FEEDBACK_TEMPLATE.md` | 首次感受、最喜欢功能、困惑点、继续意愿、停用原因 |
| `BETA_METRICS_PLAN.md` | Activation / Retention / Engagement 指标口径与采集节奏 |
| `backend/scripts/betaOps.js` | 只读指标导出与精确邮箱安全清理 |
| `backend/test/betaOps.test.js` | 指标导出与清理安全回归 |

新增命令：

```bash
cd backend
node scripts/betaOps.js metrics --db ./chenguang.db
node scripts/betaOps.js cleanup --db ./chenguang.db --email <exact-email> --yes
```

清理命令必须额外满足：

1. 环境变量 `BETA_OPS_CONFIRM=YES`。
2. 只接受精确邮箱，不做模糊匹配。
3. 如果会删除数据库内全部用户，必须显式提供 `--allow-all`。

## 2. 是否可以邀请真实用户？

**可以。**

依据：

1. 注册、登录、目标、记录、统计、AI、同步闭环已通过自动化验证。
2. 前端测试 **292 / 292 PASS**。
3. 后端测试 **62 / 62 PASS**。
4. 生产构建 **PASS**，`manifest.json` 与 `service-worker.js` 均存在。
5. Production bundle 未发现 `localhost:3000/api`。
6. Beta 测试目标、周期、反馈模板和指标口径已经建立。
7. 测试账号可通过正常注册页或 `/api/auth/register` 创建。
8. 测试数据可通过精确邮箱安全清理，避免误删真实用户。

邀请前必须确认：

1. Beta 使用独立 `DB_PATH`，不复用生产数据库。
2. `JWT_SECRET` 独立且强随机。
3. `CORS_ORIGIN` 只允许 Beta 前端域名。
4. `AI_API_KEY` 只保存在后端环境变量。
5. 用户知情：数据用于产品测试，报告只使用匿名编号。

## 3. 环境与账号能力检查

| 能力 | 当前状态 | 结论 |
|---|---|---|
| 开发 / 生产环境区分 | 已有 `NODE_ENV`、`DB_PATH`、`CORS_ORIGIN`、`JWT_SECRET` 区分 | 通过 |
| Beta 环境隔离 | 使用 `NODE_ENV=production` + 独立数据库和独立密钥 | 通过 |
| 测试账号创建 | 正常注册接口即可创建，无需新增后台 | 通过 |
| 测试数据清理 | 新增精确邮箱清理脚本，带双重确认 | 通过 |
| 指标导出 | 新增只读 JSON 指标导出 | 通过 |
| AI 调用指标 | 当前未做事件埋点，使用服务器日志人工统计 | 记录为限制 |
| 打开产品但未记录行为 | 现有口径无法精确识别 | 记录为限制 |

本阶段没有引入第三方埋点或新建事件表，避免改变架构。

## 4. 如何观察用户行为？

### 自动化指标

运行只读导出：

```bash
cd backend
node scripts/betaOps.js metrics --db ./beta-chenguang.db
```

关注：

| 类别 | 指标 |
|---|---|
| Activation | 有目标用户比例；有行为记录用户比例 |
| Retention | D+1 有记录回访比例；D+7 内有记录回访比例 |
| Engagement | 打卡 / 运动 / 阅读 / 英语 / 待办 / 专注记录总数 |
| Engagement | 目标数量与已完成目标数量 |

### 服务器日志

人工统计：

1. `POST /api/ai/chat 200` 次数，作为 AI 成功调用数。
2. `500` / `401` / `429` 数量，作为稳定性信号。
3. 核心页面请求数，辅助观察打开趋势。

### 主观反馈

按 `BETA_FEEDBACK_TEMPLATE.md` 在 D1 / D7 / D14 收集：

1. 第一次使用感受。
2. 最喜欢功能。
3. 最困惑地方。
4. 是否愿意继续使用。
5. 为什么停止使用。

## 5. Beta 判断标准

14 天后满足以下条件，说明 Beta 成功：

1. 至少 10 人完成注册。
2. 至少 70% 创建过目标。
3. 至少 80% 完成过一条行为记录。
4. 至少 4/10 用户连续使用 3 天。
5. 至少 50% 明确愿意继续使用。
6. 没有 P0 / P1 阻塞。
7. 没有数据丢失、账号隔离、JWT、AI Key 或 XSS 安全问题。

若指标未达标，优先分析反馈而不是继续加功能。

## 6. 下一阶段：继续开发还是等待反馈？

**等待真实反馈，只做小步修复。**

理由：

1. 产品功能闭环已经完成，当前最大风险不是缺少大型功能。
2. 真实用户可能反馈习惯门槛、AI 有用性、移动端碎片体验或目标理解成本。
3. 盲目继续开发会推迟最关键的留存验证。
4. Beta 结果应决定下一阶段是继续 UX 修复、强化 AI、做激励系统，还是准备公开发布。

等待反馈期间只允许：

1. P0 / P1 阻塞修复。
2. 明确的 UX / 文案 / 空态 / 引导修复。
3. 安全与稳定性修复。
4. 测试与环境配置修复。

禁止：

1. 新增大型功能。
2. 引入新框架。
3. 修改核心架构。
4. 创建第二套数据系统。
5. 在没有真实反馈的情况下开发成就系统、社交系统或复杂计划模板。

## 7. 剩余风险

| 级别 | 风险 | 处理 |
|---|---|---|
| P2 | 没有事件埋点，只打开未记录的用户无法精确识别 | Beta 阶段用页面日志与人工反馈补充 |
| P2 | AI 指标只能来自服务器日志 | 每日统计 `POST /api/ai/chat 200` |
| P2 | 小数据量下 AI 建议可能偏通用 | 通过 D3 / D7 反馈判断 |
| P2 | 真实手机体验仍需验证 | Beta 用户必须使用手机浏览器测试 |
| P3 | 目标完成状态可能未完全持久化 | 以最新快照和人工核对为准 |

## 8. 最终判断

晨光自律台已经具备真实 Beta 运营的最小能力。  
可以邀请 10–15 名真实大学生进入 14 天受控测试。  
下一阶段应优先等待反馈，不启动大型功能开发。

