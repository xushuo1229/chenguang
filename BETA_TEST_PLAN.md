# 晨光自律台 · Real Beta Test Plan

> 阶段：REAL-BETA-OPS  
> 结论：进行 10–15 人、14 天的受控真实用户测试。  
> 原则：验证是否形成“每天一次小记录”的习惯；不新增大型功能，不改核心架构。

## 1. 测试目标

1. 验证真实大学生能否完成 **注册 → 创建目标 → 完成第一次记录 → 次日回访**。
2. 验证产品在真实手机、真实课业节奏和碎片时间里的可用性。
3. 观察 AI 在小数据量下是否仍有帮助，并确认无误导或敏感信息泄露。
4. 收集首周留存、卡点、误解和功能价值反馈，决定下一阶段方向。

## 2. 用户数量与结构

| 项目 | 方案 |
|---|---|
| 目标人数 | 10–15 人 |
| 保底人数 | 10 人 |
| 周期 | 14 天 |
| 方式 | 受控邀请制，不公开推广 |

建议画像分布：

- 大一 / 适应期学生：2–3 人
- 考研学生：3 人
- 健身学生：2 人
- 高效规划学生：2–3 人
- 忙碌实习 / 课程冲突学生：2–3 人
- 曾自律失败、愿意重启的学生：1–2 人

## 3. 测试周期

| 时间 | 任务 |
|---|---|
| D0 | 邀请用户，说明隐私边界和 14 天测试范围；用户完成注册 |
| D0–D1 | 用户完成首目标或第一件 10 分钟记录 |
| D1 | 发送次日回访提示，收集第一印象 |
| D3 | 收集使用卡点、移动端问题、AI 感受 |
| D7 | 收集中期反馈，核对指标 |
| D14 | 最终反馈，判断是否进入下一阶段 |

## 4. 核心观察指标

详细口径见 `BETA_METRICS_PLAN.md`。

| 类别 | 指标 | 达标参考 |
|---|---|---|
| Activation | 首次创建目标比例 | ≥ 70% |
| Activation | 首次完成一条行为记录比例 | ≥ 80% |
| Retention | 次日回访比例 | ≥ 40% |
| Retention | 7 日回访比例 | ≥ 40% |
| Engagement | 每人平均记录次数 | ≥ 7 次 / 14 天 |
| Engagement | AI 调用次数 | 观察指标，不设硬门槛 |
| Engagement | 目标完成次数 | 观察指标 |
| Quality | P0 / P1 阻塞数 | 0 |
| Subjective | 明确愿意继续使用比例 | ≥ 50% |

> 注意：真实 Beta 优先看“能否每天完成一次小动作”，而不是只看功能点击量。

## 5. 反馈收集方式

1. 每位用户使用 `BETA_FEEDBACK_TEMPLATE.md` 提交 D1 / D7 / D14 反馈。
2. D3 可用 5 分钟文字或语音补充，不强制会议。
3. 每位用户分配匿名编号，例如 `BETA-01`；报告不公开邮箱和真实姓名。
4. 遇到崩溃、数据丢失、登录失败、AI 泄露等问题按 P0 处理。

## 6. 环境与账号策略

- 开发环境：`NODE_ENV=development`，使用本地 `backend/chenguang.db`。
- Beta 环境：`NODE_ENV=production`，但使用独立的 `DB_PATH`、独立 `JWT_SECRET`、精确 `CORS_ORIGIN` 和构建产物 `STATIC_DIR`。
- 不使用生产数据库承载 Beta 数据，除非确认该库本身仅用于 Beta。
- 用户通过正常注册页或 `/api/auth/register` 创建账号；不引入后台批量建号功能。
- 清理测试账号时使用 `backend/scripts/betaOps.js` 的精确邮箱清理命令，禁止模糊匹配真实用户邮箱。

## 7. Beta 结束判断

满足以下条件时可进入下一阶段：

1. 没有未解决的 P0 / P1 阻塞。
2. 10 人中至少 4 人能连续使用 3 天。
3. 至少 50% 明确愿意继续使用。
4. 没有出现数据丢失、账号隔离、JWT、AI Key 或 XSS 安全问题。

若反馈显示核心问题不是功能缺失，而是习惯或理解成本，则继续等待反馈并做小步 UX 修复，不启动大型功能。

